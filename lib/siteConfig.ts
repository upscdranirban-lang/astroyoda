/**
 * Single source of truth for the site's own public URL and name.
 *
 * >>> OWNER ACTION REQUIRED AFTER DEPLOYMENT (Phase 17) <<<
 * `SITE_URL` below is a placeholder. Once the site is deployed (see
 * README's "Deploying AstroYoda" section), replace it with the real,
 * live URL your hosting provider gives you (e.g. "https://astroyoda.netlify.app",
 * or your own custom domain if you set one up) — then re-run
 * `npm run build` and redeploy. This one constant feeds the canonical
 * URLs, Open Graph tags, and sitemap.xml/robots.txt used across the
 * whole site (Phase 14, SEO) — so this is the only place that needs
 * updating, instead of hunting through every file.
 */
export const SITE_URL = "https://astroyoda.example.com";

export const SITE_NAME = "AstroYoda";
